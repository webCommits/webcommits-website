{
  "name": "webcommits-website",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npm run build && node scripts/smoke-test.js",
    "smoke": "node scripts/smoke-test.js",
    "build": "npx @11ty/eleventy",
    "start": "npx @11ty/eleventy --serve",
    "deploy": "npm run build && git add docs && git commit -m 'Deploy' && git push origin HEAD"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@11ty/eleventy": "^3.1.6",
    "path-to-regexp": "^8.2.0"
  }
}
